import 'package:flutter_riverpod/flutter_riverpod.dart';

final stretchProvider = Provider<StretchService>((ref) => StretchService());

class StretchService {
  // Placeholder — stretch guide logic will live here.
}
