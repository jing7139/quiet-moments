import 'package:flutter_riverpod/flutter_riverpod.dart';

final statsProvider = Provider<StatsService>((ref) => StatsService());

class StatsService {
  // Placeholder — stats aggregation logic will live here.
}
